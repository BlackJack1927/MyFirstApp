<h2><%= @post.title%></h2>
<p> <%= @post.body%><p/>
<hr>
<%=link_to "Редактировать", edit_post_path(@post), :class => 'btn btn-warning'%>
<%=button_to "Удалить Пост", post_path(@post), method: :delete,  confirm: 'Are you sure?', :class => 'btn btn-danger'%>
<!-- <input  type = 'submit' value = "Delete"><<#%= @post.destroy %>> </input/> -->
